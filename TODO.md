# TODO

- [ ] Create an SEO hardening plan based on current files (`index.html`, `robots.txt`, `sitemap.xml`).
- [ ] Update `index.html` meta tags: add canonical, social image size, Twitter card, robots meta, theme-color.
- [ ] Improve on-page SEO: ensure one clear H1, add missing semantic landmarks (main/footer), add keyword-rich headings per section.
- [ ] Improve structured data: expand JSON-LD to include Person + sameAs + knowsAbout + worksFor (if applicable).
- [ ] Add image SEO: set explicit width/height where feasible and add lazy-loading to below-the-fold images.
- [ ] Add performance/SEO friendly settings: async/defer for non-critical scripts, preload critical CSS/hero assets if applicable.
- [ ] Validate sitemap/robots configuration consistency.
- [ ] Run a quick local validation (HTML validity + check for duplicate/incorrect meta).

